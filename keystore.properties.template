# Copy to keystore.properties (gitignored) and fill in.
# Generate the keystore with:
#   keytool -genkeypair -v -keystore tik-upload.jks -keyalg RSA -keysize 2048 \
#           -validity 10000 -alias tik-upload
storeFile=tik-upload.jks
storePassword=CHANGE_ME
keyAlias=tik-upload
keyPassword=CHANGE_ME
